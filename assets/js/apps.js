// retrieve the datas from the DOM and capitalize them.

const data = document.getElementById("statementData").innerHTML.toUpperCase();
console.log(data);


// const stringData = data.forEach(value) {
//     if(value !== number ) {
//         return string;
//     }
// };

// data.valueOf = function (value) { 
//     if (value === string

// }




// data.forEach(function letterSelected(data) {
//     if (data.element == 
// })


const tableData = [... data];
console.log(tableData);

tableData.forEach( element => console.log(element));
const str = tableData.forEach( element => (element) {
    if(tableData.element !== number) {
        return value;
    }
} );

console.log(str);

// const sortingData = tableData.sort();

// console.log(sortingData);

